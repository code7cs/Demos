/*
Problem: implement promiseAll.

Write a function that behaves like Promise.all for an iterable of values.

Requirements:
- Resolve with an array of fulfilled values in the original input order.
- Accept both promises and non-promise values.
- Resolve with [] for an empty iterable.
- Reject as soon as any input rejects.

Run the solution tests:
  node async-javascript/promise-all/tests.js

After implementing this file, run:
  node async-javascript/promise-all/tests.js exercise
*/

function promiseAll(values) {
  throw new Error('TODO: implement promiseAll');
}

module.exports = promiseAll;
