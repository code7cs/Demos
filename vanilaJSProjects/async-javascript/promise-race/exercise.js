/*
Problem: implement promiseRace.

Write a function that behaves like Promise.race for an iterable of values.

Requirements:
- Settle with the first input that fulfills or rejects.
- Accept both promises and non-promise values.
- Leave the returned promise pending for an empty iterable.

Run the solution tests:
  node async-javascript/promise-race/tests.js

After implementing this file, run:
  node async-javascript/promise-race/tests.js exercise
*/

function promiseRace(values) {
  throw new Error('TODO: implement promiseRace');
}

module.exports = promiseRace;
